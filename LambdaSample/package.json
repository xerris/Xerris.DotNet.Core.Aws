{
  "name": "calgary-library-microservice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "Xerris Inc.",
  "license": "ISC",
  "scripts": {
    "deploy": "gulp Package && cd src/Infrastructure && npm install && npm run build && cdk deploy --profile xerris && cd ../..",
    "destroy": "cd src/Infrastructure && npm install && cdk destroy --profile xerris && cd ../.."
  },
  "dependencies": {
    "@aws-cdk/aws-iam": "^1.15.0",
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.2.0",
    "gulp-zip": "^5.0.1",
    "mkdirp": "^0.5.1",
    "npm": "^6.12.1",
    "path": "^0.12.7",
    "shell": "^0.5.0",
    "yargs": "^14.2.0"
  },
  "devDependencies": {
    "gulp-dotnet-cli": "^1.1.0"
  }
}
